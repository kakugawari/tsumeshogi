# 詰将棋チェッカー

盤に駒を並べて「詰みチェック」を押すと、詰むかどうか・何手詰めか・
手順を調べてくれるアプリ。詰将棋の本の問題を自分で並べて答え合わせしたり、
自分で作った問題を検討する (詰みか、余詰めが無いか) のに使う。

やねうら王のような外部エンジンには依存しない。全ての判定・探索は
`core.js` の自前ロジック (王手だけを許す OR/AND 探索 + 反復深化) で行う。
理由は README ではなく `app_template` の CLAUDE.md を参照 (ビルドを挟まず、
1ファイルずつ差し替えられる作りを保つため)。

盤と駒の見た目は `kakugawari/kifu` (棋譜ノート) と共通にしてある。
- 駒の字は kifu と同じ埋め込みフォント (Shippori Mincho Bold から14文字だけ
  切り出した woff2, `styles.css` に base64 で埋め込み)。詳細とライセンスは
  `fonts/README.md`
- 盤・駒台の色 (`--board` `--board-line` `--piece-ink`) も kifu の値
  (`--ban` `--ban-line` `--sumi`) に合わせてある
- アイコンも kifu と同じ作り (和紙の地 + 五角形の駒 + 一文字)。字だけ
  「棋」→「詰」に変えてある。`tools/make-icons.js` で作りなおせる
  (kifu の同名スクリプトを字だけ差し替えたもの)
- **盤に描く字と、棋譜に書く呼び名は分けてある** (`core.js` の
  `pieceDisplayName` / `pieceKifuName`)。盤は本物の駒札と同じ一文字
  (と・杏・圭・全・馬・龍)、棋譜は読み下しの決まりどおり
  「成香・成桂・成銀」と書く。埋め込みフォントが「成」の字を含まないので、
  盤の上で二文字表記にはできない (棋譜は画面の地のフォントなので問題ない)

---

## 動かす・試す

```bash
npm start          # http://localhost:8080/ で開く
npm test           # ロジックのテスト (node だけで動く。速い)
npm run test:ui    # ブラウザで実際に動かすテスト (要 npm i -D playwright)
```

`npm run test:ui` は本物のブラウザを立ち上げ、指の操作をそのまま再現する。
**画面を触ったときの不具合はこちらでしか捕まらない。** 変更したら必ず両方流す。

## 構成

```
index.html / styles.css   画面 (駒台・盤・駒箱・画像の読み取り・結果表示)
core.js                    将棋のルールと詰み探索。DOM を触らない。node と
                            ブラウザの両方、そして Worker の中でも動く
recognize.js               盤面の画像を読み取る。DOM を触らない
app.js                     盤の編集・駒台の編集・画像の受け取り・詰みチェックの呼び出し
solver-worker.js           詰み探索を別スレッドで行う (画面を固まらせないため)
core.test.js                ロジックのテスト (node --test)
recognize.test.js           読み取りのテスト (node --test)
browser-test.js             ブラウザで動かすテスト
serve.js                    開発用サーバー
manifest.webmanifest        ホーム画面に追加するときの名前とアイコン
icon-*.png / apple-touch-icon.png   アイコン
tools/make-icons.js         アイコンを作りなおすスクリプト (Playwright が要る)
fonts/                      駒のフォントの出どころとライセンス
```

ファイルはすべて直下に置く。増えてきたら `src/` などに分けてよい
(そのときは上の表と読み込み先も直す)。

`window.__app` はテストから中身をのぞくための入口。`browser-test.js` が使う。

## 盤面の表し方 (決めごと)

- 盤は 81 マスのフラット配列。筋(file) 1〜9・段(rank) 1〜9。SFEN と同じ並び順。
- 駒は `'P','L','N','S','G','B','R','K'` (先手=大文字) / 小文字(後手)。
  成り駒は先頭に `+`。王だけは成らない。
- 先手は rank が減る方向 (1 段目) へ進む。後手はその逆。色によって
  向きを反転する処理は `core.js` の `pieceTargets` に集約している。
- SFEN の読み書き (`parseSfen` / `toSfen`) に対応。本の問題をそのまま
  貼り付けたり、作った局面を書き出したりできる。

## 盤の作り方 (画面の決めごと)

ぴよ将棋の「局面編集」と同じ形にしてある。駒パレットは持たず、
**駒台がそのまま駒の置き場をかねる。**

- 最初の局面は「玉方の玉が5一・残りの駒一式は玉方の駒台」。詰将棋の
  決めごと (盤に出していない駒はすべて玉方の持ち駒) と同じ形なので、
  ここから駒を移していけば自然に問題の形になる。攻方の玉は要らない
- 置き方は1通りだけ: **駒台の駒をタップして持つ → 盤をタップして置く**。
  盤に置いた駒はまず「攻め」(先手・不成) になる
- 盤の駒は**1回目のタップで選ぶ**。選んだあと
  - **空いたマスをタップすると、そこへ動かす** (動かしたあとも選ばれたまま)
  - **同じ駒をもう一度タップすると、向きと成りが
    攻め → 守り → 成った攻め → 成った守り → 攻め … と回る** (`cyclePiece`)。
    金と玉は成れないので攻め↔守りの2つだけ
  - 別の駒のあるマスをタップしたときは、動かさずに選び直しになる
    (駒の上に駒を重ねて消してしまわないため)
- 攻方の持ち駒は、玉方の駒台から駒を持って攻方の駒台の「ここへ置く」で渡す
- 「消す」を押してから盤の駒をタップすると、その駒は玉方の駒台に戻る
  (駒の枚数が増えたり減ったりしないよう、盤から出した駒は必ず駒台へ返す)
- 玉は枚数を数えず、「駒箱」の玉から何枚でも置ける。2枚以上置いたら
  `validatePosition` がエラーとして報告する

**玉方の駒台に残した駒は、玉方が合駒に使える。** これは詰将棋の決めごと
どおりの動きで、同じ配置でも玉方の持ち駒の有無で手数が変わる
(`core.test.js` の「玉方に持ち駒があると、合駒のぶん詰みが延びる」)。
使わせたくない駒は攻方の持ち駒へ渡す。

## 画像から盤面を読み取る (`recognize.js`)

詰将棋の盤面を写した画像を貼り付けると、盤に並べ直す。外部のサービスには
一切送らない。**手本の字はこのアプリが持っている埋め込みフォントで描く**ので、
ビルドも通信も要らず、Artifact でもオフラインでも同じように動く。

1. **格子を見つける。** 将棋盤の線は「等間隔に10本」並ぶ。この並びを一番よく
   説明する 間隔 と 位置 を総当たりで探す (`findComb`)。駒や画面の他の部分が
   作る線に惑わされにくく、画面まるごとのスクリーンショットからでも盤を拾える。
2. **マスごとに字のインクを取り出す。** 閾値は1つに決め打ちせず何通りか試し、
   **マスのふちに繋がったインクは捨てる** (`dropBorderTouching`)。駒札の縁や
   格子線を字と一緒に囲ってしまう失敗を、これで防いでいる。
3. **駒札の向きは五角形の形で決める** (`komaDirection`)。「王」のように180度
   回してもほとんど同じ字があるので、字の向きだけでは先手後手を決められない。
   札を描かず字だけを置く盤 (このアプリ自身がそう) では「分からない」を返し、
   字の向きに任せる。
4. **字は手本と重ね合わせて選ぶ** (`similarity` は Jaccard)。手本は書体を
   3通り用意し、正立と180度回したものの両方を持つ。

読み取れた自信は `score` (手本との近さ)・`margin` (別の駒との差)・
`orientationSure` (向きの確からしさ) で表す。自信の低いマスは盤に赤い点を
出して、目で確かめてもらう。**100%は狙わない。直すのは人の手が速い** ので、
読み取ったあとはそのまま手で直せる形にしてある。

持ち駒は読み取らない。盤に出ていない駒は詰将棋の決めごとどおり玉方の駒台に
入るので、攻方の持ち駒はそこから渡してもらう。

実測 (`npm test` / `npm run test:ui` に見張りを置いてある):
- 自分の盤を撮って読み直す往復: 81/81 マス
- 別の描き方の盤 (五角形の札・別の書体・別の色・JPEG) 30局面: 2425/2430 (99.8%)
- 画面まるごとのスクリーンショット: 80/81 マス
- かかる時間: 0.3〜0.5秒

## 詰み探索の考え方

- 攻方 (手番側) は**王手になる手しか探索しない** (OR ノード: 1つでも
  詰みに繋がれば良い)。玉方はどんな合法手でも良い (AND ノード: 全ての
  応手が詰みに繋がって初めて成功)。
- 1手詰め→3手詰め→5手詰め…と手数を伸ばしながら探す (反復深化)。
  最初に見つかった手数がそのまま最短手数になる。
- 同一局面の繰り返し (連続王手の千日手) は、探索経路上の Set で検出して
  攻方の失敗として扱う。
- 打ち歩詰めは「その歩打ちが王手になり、かつ相手の合法手が0になるか」を
  実際にシミュレートして判定している (机上のルール表ではなく実測)。
- 手数・局面数に上限を設けている (画面からは 41手詰めまで・200万局面まで)。
  それを超えると「打ち切り」として報告する。
  **効いているのは手数より駒数。** 実測で探索は約19,000局面/秒しか出ず、
  200万局面を使い切ると約105秒かかる。1手詰めは5局面・3手詰めは109局面で
  即座に終わるが、攻方の持ち駒が1枚増えるだけで枝が桁で増える
  (持ち駒2枚の不詰み判定で27万局面・26秒)。快適なのは駒数の少ない
  5〜7手詰めくらいまで。速くするなら `legalMoves` (王手判定のたびに
  盤81マスを走査している) が最初の的。
- 見つかった詰み手順と同じ手数で詰む「別の初手」がないかも調べて、
  あれば「余詰めの疑い」として警告する。

## テストプレイ

`npm run test:ui` の中でスマホ幅・明暗両テーマ・実際の駒操作を自動で
確かめている。加えて、1手詰め・3手詰め・不詰み・打ち歩詰めの局面を
実際に盤に並べてスクリーンショットで目視確認済み。

Artifact での配布は `app_template` の手順 (全ファイルを1枚のHTMLにまとめて
公開) に従う。Worker はファイル分割できないので、Blob URL 経由で core.js
の中身を文字列として埋め込んでいる (ビルドスクリプトは `scratchpad` 側の
使い捨てで、リポジトリには含めない)。

---

## 踏んだ落とし穴

`app_template` の表に加えて、このアプリで踏んだもの:

| 症状 | 原因 | 直し方 |
| --- | --- | --- |
| 後手の駒が一切動けない (合法手が0になる) | `pieceTargets` が `baseType(piece)` (元の大文字/小文字を保つ) を `switch` の大文字リテラルと比較していた。後手 (小文字) の駒だけ全ての `case` に一致せず、常に空の手を返していた | 比較用に `.toUpperCase()` してから `switch` に渡す (色の情報は別途 `colorOf()` で持つ) |
| 「1手詰め」のつもりで作った局面が、意図と違う手順で詰んでしまう | 王手を防ぐために置いた駒 (金など) が、たまたま持ち駒の駒を打つ手を「守って」しまい、想定より早い/別の手順の詰みが生まれていた (実質的な余詰め)。手で読み切ったつもりでも、成り・別ルートの王手を見落としやすい | 局面を手で検証する代わりに、エンジン自身に `findMate` を実際に走らせて、見つかった手順を `describePv` で再生し、想定通りか確認する。テストも「特定の手」ではなく「見つかった手順が本当に詰みまで進むか」を再生して検証する形にする (`assertGenuineMate`) |
| 駒パレットの後手側ボタンが読みにくい | 盤上の駒と同じ CSS で180度回転させていたため、ボタンの文字も逆さまになり選びにくかった | 盤上の駒 (向きの情報) と、駒を選ぶボタン (ただの操作) は別。回転させず、ラベルで区別する (このパレット自体は後に廃止。下の行を参照) |
| 「駒の置き方がわからない」と言われた | 空の盤 + 駒パレット + 持ち駒の +/- ボタン、と入力の仕方が3種類あり、どれで何をするのか説明も無かった。加えて攻方の玉が無いと詰みチェックが必ずエラーになっていたが、実際の詰将棋の図面は攻方の玉を省くのが普通で、そこでも詰まっていた | **駒パレットを無くし、駒台をそのまま駒の置き場にした** (ぴよ将棋の局面編集と同じ形)。置き方は「駒台の駒をタップして持つ→盤をタップ」の1通りだけにし、向き・成りは盤の駒をタップして回す (`cyclePiece`)。最初から玉方の玉が5一・残りの駒一式が玉方の駒台に入った状態で始める。攻方の玉は無くても詰みチェックできるようにした (`validatePosition`/`findMate` は玉方の玉だけを必須にする) |
| 駒を1枚置いただけで盤が上下にずれる | 玉方の駒台を盤の**上**に置いたため、駒が減って札の折り返し行数が変わると、その下の盤ごと動いてしまった | 駒台の札の入れ物に `min-height` で最初の行数ぶんの高さを先に確保しておく。「反対の駒台へ渡す」ボタンも、持っていないときは消さずに押せない状態で出しておく (出し入れで高さが変わらないように) |
| 棋譜に「▲2二全」と出て読めない | 盤に描く駒札の字 (`pieceDisplayName`) をそのまま棋譜にも使っていた。駒札の一文字略称 (全・圭・杏) は駒の上の表記であって、棋譜の書き方ではない | 盤用と棋譜用で呼び名を分ける (`pieceDisplayName` / `pieceKifuName`)。棋譜は「成銀・成桂・成香」。埋め込みフォントに「成」が無いのは盤だけの制約で、棋譜は画面の地のフォントなので二文字で書ける |
| 同じ配置なのに、想定より詰みの手数が長く出る | 玉方の駒台に残した駒を、玉方が**合駒**として使えるため。詰将棋の決めごと (盤に出していない駒はすべて玉方の持ち駒) どおりの正しい動きだが、駒台に駒を残したまま検討すると想定と食い違う | 仕様として正しいので直さない。画面に「玉方の駒台に残った駒は玉方が合駒に使えます」と明記し、使わせたくない駒は攻方の持ち駒へ渡してもらう。この違いが消えないよう `core.test.js` に見張りを置いた |
| 画像を読み取ると、駒の形が潰れて当たらない | 駒札の縁 (中間色) をインクとして拾い、字を囲む枠ごと正規化していた。成り駒は赤字で縁と明るさが近く、特に起きやすい | 字はマスの真ん中に浮いているが、札の縁・格子線・影は必ずマスのふちに届く。**ふちに繋がったインクを消す** だけで直った (`dropBorderTouching`)。閾値も1つに決め打ちせず何通りか試し、字らしい (詰まり具合の高い) ものを選ぶ |
| 後手の玉を先手と読み違える | 「王」「玉」は180度回してもほとんど同じ形。字の照合では向きが決まらないのに、点数の高いほうを黙って採っていた | 駒札の**五角形の向き**で決める (`komaDirection`)。どの駒でも同じ理屈で決まる。札を描かない盤では「分からない」を返し、字の向きに任せる |
| 札の向き判定が常に「上向き」を返す | 判定にマス全体を使っており、**格子線を札と見間違えて**いた。線のぶんで「札らしい画素」の割合が水増しされ、上下の幅の比べ合いも線に支配されていた | 少し内側だけを見る。あわせて「札らしい画素」が一定の割合に満たなければ「分からない」を返す (字だけを置く盤ではここに入る) |
| 「自信なし」の印が全部のマスに付く | 2番手との差で測っていたが、同じ駒の手本を書体ちがいで3枚持っているため、2番手はいつも同じ駒になり差が0だった | **別の駒**のうち一番あてはまるものと比べる (`classify` の margin) |

| ホーム画面に追加すると違うアイコンが出る | iOS は `apple-touch-icon` に SVG を使えない (`app_template` の表にもある既知の落とし穴)。雛形のまま `icon.svg` だけ置いていた | 180x180 の PNG を用意して `apple-touch-icon` に指定する。Android 用には manifest に 192/512 と maskable を並べる。`browser-test.js` に「PNG か・中身も本当に PNG か・180x180 か・manifest のアイコンが全部配信されるか」の見張りを置いた |

**新しく踏んだら、この表に足すこと。**
